export function isError(day: number, month: number, year: number): boolean {
	// Vérifier les inputs (Gérer les erreurs)
	// if (month < 1 || month > 12) {
	// 	const errorMonth = document.querySelector(".errorMonth") as HTMLParagraphElement;
	// 	errorMonth.innerHTML = "Error Month";
	// 	errorMonth.style.display = "block";
	// }
	// Vérifier le nombre de jour dans tel mois
	// Vérifier si mois est compris entre 1 et 12
	// Vérifier si l'année est dans le futur ou trop dans le passé ?
	// Vérifier que cela est bien des nombres !
	return false;
}
